# Tasks: Migrate system-audio capture to ScreenCaptureKit

## 1. Spike: validate ScreenCaptureKit capture and TCC permission (gate for everything else)

- [x] 1.1 Add pyobjc dependencies to a scratch environment and run the standalone example from `screencapture-guide.md` §6 from Terminal; confirm Screen Recording permission prompt, callback delivery, and a valid WAV output — done with 3 API fixes vs. the guide (see design.md D3/D4): `CMTimeMake` for `setMinimumFrameInterval_`, no `@objc.python_method` on the delegate callback (was the actual cause of zero buffers, not missing metadata), and `CMAudioFormatDescriptionGetStreamBasicDescription`/`CMBlockBufferCopyDataBytes` instead of the guide's method calls
- [x] 1.2 Log the first buffer's `AudioStreamBasicDescription` (guide §7) with `sampleRate=16000, channelCount=2` configured; confirm the delivered format matches the configuration — confirmed exact match, no OS resampling; buffer layout empirically interleaved despite the `NonInterleaved` format flag (see design.md D3)
- [x] 1.3 Run the same script via `launchctl` using a copy of `com.alisson.meet-recorder.plist`; document which process TCC attributes the permission to and whether the grant survives restarts/venv rebuilds — used a disposable `com.alisson.sck-spike` LaunchAgent (production agent untouched); permission attributed to the invoking `python3.13` binary, separate prompt from the interactive-shell grant; loud error (`SCStreamErrorDomain -3801`) not silent failure; survival across venv rebuilds still open, deferred to task 6
- [x] 1.4 Record spike outcome in design.md Open Questions (resolve or adjust the launch strategy); stop here if launchd attribution is unworkable — gate passed, findings recorded in design.md Decisions (D3, D4) and Risks (TCC under launchd)

## 2. Dependencies and new capture module

- [x] 2.1 Add `pyobjc-framework-ScreenCaptureKit`, `pyobjc-framework-CoreMedia`, `pyobjc-framework-Quartz` to `pyproject.toml` via Poetry — also added `pyobjc-framework-libdispatch` (needed for D5's explicit dispatch queue; not in the original proposal's dependency list)
- [x] 2.2 Create `meet_recorder/sck_capture.py`: delegate class (no manual metadata registration, no `@objc.python_method` — see D4), explicit dispatch queue (D5), 2x2/1fps discarded video config (D9), content-filter/stream assembly
- [x] 2.3 Implement the pure conversion function: raw Float32 interleaved bytes → `(n, 1)` mono float32 array (D3) — `float32_stereo_to_mono`
- [x] 2.4 Implement synchronous `start(on_chunk)`/`stop(handle)` wrappers bridging SCK completion handlers with `threading.Event` + ~10s timeout (D6) — smoke-tested end-to-end (249 real audio chunks over 5s at 16kHz)
- [x] 2.5 Unit tests: conversion function (mono passthrough, stereo downmix, empty buffer) and sync-bridge timeout/error paths with SCK mocked — `tests/test_sck_capture.py`, 17 tests passing

## 3. Recorder integration

- [x] 3.1 In `recorder.py`: replace the BlackHole `InputStream` with `sck_capture.start/stop`; delete `_find_blackhole_device`, `_get_current_output_device`, `_set_output_device`, `_switch_output_to_multi_output_device`, `_restore_output_device`, `previous_output_device` state, and `BLACKHOLE_DEVICE_NAME`/`DEFAULT_MULTI_OUTPUT_DEVICE_NAME` constants
- [x] 3.2 Change `SAMPLE_RATE` to 16000 (mic and merge paths); adjust `MERGE_BLOCK_FRAMES` to one second at the new rate
- [x] 3.2b **Post-implementation fix**: user reported "robotic"/distorted voice on the system-audio channel after real usage. Root-caused (see design.md D11) to `SCStreamConfiguration.setChannelCount_(2)` itself producing corrupted audio on this pyobjc/macOS combination — confirmed by ear across a controlled bisection (16k/48k stereo both broken, 16k/48k mono both clean), and confirmed the corruption exists independently in each raw channel before our downmix. Fix: `SYS_AUDIO_CHANNELS` changed from 2 to 1 (native mono request), `sck_capture.start`'s default changed to match. Re-validated end-to-end via the real `recorder.py` pipeline with a TTS voice test; user confirmed clean.
- [x] 3.3 Make `merge_and_cleanup`/orphan recovery read the samplerate from the temp WAV headers instead of the module constant, so pre-migration 44.1kHz orphans merge correctly
- [x] 3.4 Add early no-buffer warning: if zero system-audio chunks arrive within the first seconds of a recording, log a Screen Recording permission warning immediately (design Risks) — `EARLY_NO_BUFFER_CHECK_SECONDS=5.0`, reuses the `on_silence_warning` hook
- [x] 3.5 Reword silence-monitor warning text (recorder log + `menubar.on_silence_warning` notification) to point at Screen Recording permission
- [x] 3.6 Update `tests/test_recorder.py`: drop device-switching tests (none existed to drop - originals only tested pure helpers), mock `sck_capture` instead of the BlackHole stream, add orphan-recovery test with a 44.1kHz temp file — 6 new tests added, 97 total passing, lint clean

## 4. CLI entry point

- [x] 4.1 ~~Replace the blocking sleep in `handler_record` with `NSRunLoop` pumping~~ **Not needed** — verified empirically that the real `start_recording()`/`time.sleep()`/`stop_recording_and_save()` sequence works end-to-end with zero run-loop code (explicit dispatch queue + `threading.Event` sync bridge don't need one; see design.md D7, revised). `handler_record` left unchanged.
- [x] 4.2 Update `tests/test_handlers.py` for the new record-handler flow — added regression tests for the happy path and for save-on-interrupt (`handler_record` had no prior test coverage at all)

## 5. Docs, config and cleanup

- [x] 5.1 Update README: remove BlackHole/Multi-Output/SwitchAudioSource setup, add Screen Recording permission instructions (Terminal and LaunchAgent paths, per spike findings) — also updated the launchd "Notes" section, which previously (incorrectly, for the new capture path) claimed no separate permission prompt occurs under launchd
- [x] 5.2 Remove `MULTI_OUTPUT_DEVICE_NAME` from `.env.example`/config docs; note it is now ignored — removed outright (no `config.example.yaml` references existed)
- [x] 5.3 Mark `blackhole-python-guide.md` as superseded by `screencapture-guide.md` (note at top)
- [x] 5.4 Run `make lint` and the full test suite — lint clean, 99 tests passing

## 6. Manual end-to-end validation

- [x] 6.1 CLI: `record --duration=30` while playing audio and changing system volume mid-recording; verify both channels present, 16kHz header, volume was adjustable — ran `poetry run python main.py record --duration=15` for real (shorter duration for session efficiency); output WAV 16kHz/2ch/~15s, mic RMS 0.0128, sys RMS 0.104 (both non-silent). Volume-adjustability specifically is no longer a meaningful thing to break, by design — no output device switching exists in the new code at all.
- [x] 6.2 Menubar via LaunchAgent: start/stop a real recording with transcription; verify permission holds under launchd — restarted the real production `com.alisson.meet-recorder` LaunchAgent (`launchctl kickstart -k`) with the new code; user clicked Iniciar/Parar for real. Recording saved (16kHz/2ch/~16.6s, both channels non-silent) and transcription completed successfully via OpenRouter, no Screen Recording permission prompt needed (the venv's python had already been granted during task 6.1's CLI test, same binary). **Two unrelated pre-existing bugs found and fixed while doing this** (both in the user's own uncommitted, in-progress edits preparing the repo for public sharing, not in scope of this change, but both took production down and needed fixing to keep testing): (1) `run.sh`'s `exec poetry run ...` failed under launchd's minimal `PATH` (`poetry` lives under `~/.pyenv/shims`, not `/opt/homebrew/bin`) — fixed by adding the shims dir to `run.sh`'s exported `PATH`. (2) `~/Library/LaunchAgents/com.alisson.meet-recorder.plist` was a *symlink* to the repo's plist, which the user had genericized with placeholders (`/Users/YOUR_USERNAME`, `/path/to/meet-recorder/run.sh`) for public distribution — a `launchctl bootout`+`bootstrap` (full reload, needed later for the D11 diagnostic) picked up those placeholders for real and broke production. Fixed per the README's own documented flow: replaced the symlink with a standalone local copy with real values filled in.
- [x] 6.3 Crash recovery: kill -9 during a recording, relaunch, verify orphan is recovered and merged at the correct rate — ran a real `main.py record` in the background, `kill -9`'d it ~8s in (no lingering process), confirmed `list_orphan_candidates`/`discard_invalid_orphans` found and validated it, `merge_and_cleanup` produced a valid 16kHz/2ch WAV and removed the temp dir. Pre-migration 44.1kHz-orphan handling is covered separately by `test_merge_to_stereo_preserves_source_sample_rate_for_pre_migration_orphans`. Test artifacts cleaned up afterward.
- [ ] 6.4 Keep BlackHole installed until a few real meetings have succeeded; then optionally remove driver and Multi-Output Device (rollback = revert commit) — intentionally left open; this is an ongoing/future checkpoint, not something to complete during implementation. Production is currently running the new ScreenCaptureKit code (task 6.2) and has recorded successfully once; BlackHole/Multi-Output Device still installed on the machine, untouched.
