## 1. Capture-session pause lifecycle

- [x] 1.1 Add the capture module's whole-recording paused state and one lock serializing start, whole-recording pause/resume, microphone-only pause/resume, save, and discard; guard invalid state combinations, including whole-recording pause during microphone switching, as no-ops. Verify with recorder unit tests that both streams stop, resume writes into the same queues and temporary files, overlapping lifecycle requests execute serially, and invalid transitions do not mutate capture state.
- [x] 1.2 Prevent the padding loop, silence monitor, and early system-buffer check from generating audio or warnings during a whole-recording pause while retaining the existing microphone-only device-switch behaviour. Invalidate racing early-buffer callbacks on pause, start a new complete early-buffer window after resume only if no system buffer has ever arrived, and reset stale silence timing/buffers on successful resume; verify a pause longer than both warning windows produces no warning or padding and cannot cause an immediate stale warning after resume.
- [x] 1.3 Make `resume_recording` transactional: keep new streams local until both are active, close every resource opened by a failed attempt, and publish active state only after complete success. Try the previous microphone first; if it fails, refresh devices, retry the current default, update the selected device, and report the non-fatal fallback. Verify failures while starting either source leave both channels stopped and the session resumable or finalizable, and verify disconnected-device fallback resumes both channels.
- [x] 1.4 Make save and discard finalize an active or paused capture session safely, including ScreenCaptureKit handle ownership and final alignment, and ensure a queued or stale transition cannot reactivate the finalized session. Verify with recorder and ScreenCaptureKit tests that paused save produces one stereo WAV, paused discard removes the temporary session without output, and overlapping finalization and resume cannot publish a live handle afterward.

## 2. Menu bar controls and visual state

- [x] 2.1 Add paused and transition-in-progress state to the menu bar app, with an asynchronous Pause/Resume action whose label, callback, and enabled state match active, paused, idle, success, failure, and microphone-fallback outcomes. Disable pause/resume, both stop actions, discard, microphone switching, and quit while a whole-recording transition is in flight, and ignore stale worker completions; verify action enablement, title changes, fallback alerts, failure recovery, and stale-completion handling in `tests/test_menubar.py`.
- [x] 2.2 Keep stop, stop-without-transcription, discard, and quit available for stable paused recordings while disabling microphone switching during global pause or transition and disabling whole-recording pause during a microphone switch. Verify each paused-session finalizer resets the UI correctly, quit saves a paused session, and a late microphone-switch completion after save or discard cannot replace idle state or reopen capture.
- [x] 2.3 Add fixed-size paused and paused-plus-transcribing menu bar assets, register them in the icon state set, and select them distinctly from active and idle states without changing microphone-attention blinking; verify icon-state tests and inspect that the assets load at the existing configured point size.

## 3. Regression validation

- [x] 3.1 Run the focused capture, ScreenCaptureKit, and menu bar test modules (`pytest tests/test_recorder.py tests/test_sck_capture.py tests/test_menubar.py`) and fix any regressions in existing start, device-switch, transcription, or discard flows.
- [x] 3.2 Run the full automated suite (`pytest`) and validate the change artifacts with `openspec validate add-pause-resume-recording --strict`.
